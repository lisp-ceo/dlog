name: tests

on:
  [push]

jobs:
  gen:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-go@v2
      with:
        go-version: ~1.15
    - uses: actions/checkout@v2
    - run: make build
    - run: git diff --exit-code
